# OntoBricks — Databricks Apps deployment
# Install uv plus the local project so Databricks Apps resolves
# all runtime dependencies from pyproject.toml at build time.
# See: https://docs.databricks.com/aws/en/generative-ai/mcp/custom-mcp
uv
-e ".[lakebase,pitfalls]"
